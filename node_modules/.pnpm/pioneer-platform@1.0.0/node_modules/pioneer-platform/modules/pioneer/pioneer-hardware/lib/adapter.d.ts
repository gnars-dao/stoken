import { Keyring } from "@bithighlander/hdwallet-core";
import * as HID from "node-hid";
export declare class HIDKeepKeyAdapter {
    keyring: Keyring;
    constructor(keyring: Keyring);
    static useKeyring(keyring: Keyring): HIDKeepKeyAdapter;
    initialize(devices?: HID.Device[], autoConnect?: boolean): Promise<number>;
    protected handleConnectKeepKey(device: HID.Device): void;
    protected handleDisconnectKeepKey(device: HID.Device): void;
}
