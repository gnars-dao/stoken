import type { InitOptions, Event as AnalyticsEvent, App, ReportConfig } from './types';
export declare class Analytics<T extends AnalyticsEvent> {
    private enabled;
    private useQueue;
    private queue;
    private version;
    private app;
    private instanceId?;
    private sessionId?;
    private commitId?;
    private url?;
    private callbacks?;
    constructor(version: string, app: App);
    init: (enabled: boolean, options: InitOptions) => void;
    enable: () => void;
    disable: () => void;
    isEnabled: () => boolean;
    report: (data: T, config?: ReportConfig) => void;
}
//# sourceMappingURL=analytics.d.ts.map