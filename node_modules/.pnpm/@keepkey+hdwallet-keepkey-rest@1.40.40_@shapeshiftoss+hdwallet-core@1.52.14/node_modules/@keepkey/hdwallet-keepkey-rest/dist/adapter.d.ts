import * as core from '@shapeshiftoss/hdwallet-core';
import type { KeepKeySdk } from './kkrest';
export declare class KkRestAdapter {
    keyring: core.Keyring;
    currentDeviceID?: string;
    private constructor();
    static useKeyring(keyring: core.Keyring): KkRestAdapter;
    initialize(): Promise<number>;
    pairDevice(sdk: KeepKeySdk): Promise<core.HDWallet>;
}
