import type { EVMChain } from '@pioneer-platform/types';
import type { EVMWalletOptions } from '@pioneer-platform/types';

export declare const evmWallet: {
    connectMethodName: "connectEVMWallet";
    connect: ({ addChain, config: { covalentApiKey, ethplorerApiKey }, }: {
        addChain: any;
        config: EVMWalletConfig;
    }) => (chains: EVMChain[], walletType?: EVMWalletOptions) => Promise<boolean>;
};

declare type EVMWalletConfig = {
    ethplorerApiKey?: string;
    covalentApiKey?: string;
};

export { }
