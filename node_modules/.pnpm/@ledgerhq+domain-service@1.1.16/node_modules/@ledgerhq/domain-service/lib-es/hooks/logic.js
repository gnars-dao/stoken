// Expiration date depend on the resource's status.
export function isOutdated(resource) {
    const now = Date.now();
    switch (resource.status) {
        case "loaded": {
            return now - resource.updatedAt > 60 * 1000; // 1 minute
        }
        case "error": {
            return now - resource.updatedAt > 30 * 1000; // 30 seconds
        }
    }
    return false;
}
export const isLoaded = (domain) => domain.status === "loaded";
export const isError = (domain) => domain.status === "error";
//# sourceMappingURL=logic.js.map