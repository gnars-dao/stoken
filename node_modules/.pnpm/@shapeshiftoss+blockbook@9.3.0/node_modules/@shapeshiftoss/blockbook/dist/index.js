"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __exportStar = (this && this.__exportStar) || function(m, exports) {
    for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports, p)) __createBinding(exports, m, p);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getAddresses = exports.Blockbook = void 0;
var controller_1 = require("./controller");
Object.defineProperty(exports, "Blockbook", { enumerable: true, get: function () { return controller_1.Blockbook; } });
__exportStar(require("./models"), exports);
__exportStar(require("./websocket"), exports);
/**
 * returns a unique set of all addresses found in the associated blockbook transaction
 */
function getAddresses(tx) {
    const addresses = [];
    tx.vin?.forEach((vin) => {
        if (!vin.isAddress)
            return;
        if (!vin.addresses)
            return;
        addresses.push(...vin.addresses);
    });
    tx.vout?.forEach((vout) => {
        if (!vout.isAddress)
            return;
        if (!vout.addresses)
            return;
        addresses.push(...vout.addresses);
    });
    tx.tokenTransfers?.forEach((transfer) => {
        transfer.from && addresses.push(transfer.from);
        transfer.to && addresses.push(transfer.to);
    });
    return [...new Set(addresses)];
}
exports.getAddresses = getAddresses;
//# sourceMappingURL=index.js.map