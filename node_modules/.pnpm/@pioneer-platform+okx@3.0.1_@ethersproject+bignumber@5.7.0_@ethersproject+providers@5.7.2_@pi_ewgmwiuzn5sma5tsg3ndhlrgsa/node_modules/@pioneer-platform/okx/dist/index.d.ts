import { Chain } from '@pioneer-platform/types';

declare const OKX_SUPPORTED_CHAINS: readonly [Chain.Avalanche, Chain.BinanceSmartChain, Chain.Bitcoin, Chain.Ethereum, Chain.Cosmos];

declare type OKXConfig = {
    covalentApiKey?: string;
    ethplorerApiKey?: string;
    utxoApiKey?: string;
};

export declare const okxWallet: {
    connectMethodName: "connectOkx";
    connect: ({ addChain, config: { covalentApiKey, ethplorerApiKey, utxoApiKey }, }: {
        addChain: any;
        config: OKXConfig;
    }) => (chains: (typeof OKX_SUPPORTED_CHAINS)[number][]) => Promise<boolean>;
};

export { }
