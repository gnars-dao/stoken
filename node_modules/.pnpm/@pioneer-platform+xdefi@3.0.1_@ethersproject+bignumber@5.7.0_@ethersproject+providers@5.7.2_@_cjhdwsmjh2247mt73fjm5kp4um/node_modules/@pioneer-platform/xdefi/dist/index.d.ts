import { Chain } from '@pioneer-platform/types';

declare const XDEFI_SUPPORTED_CHAINS: readonly [Chain.Avalanche, Chain.Binance, Chain.BinanceSmartChain, Chain.Bitcoin, Chain.BitcoinCash, Chain.Dogecoin, Chain.Ethereum, Chain.Litecoin, Chain.THORChain];

declare type XDEFIConfig = {
    covalentApiKey?: string;
    ethplorerApiKey?: string;
    utxoApiKey?: string;
};

export declare const xdefiWallet: {
    connectMethodName: "connectXDEFI";
    connect: ({ addChain, config: { covalentApiKey, ethplorerApiKey, utxoApiKey }, }: {
        addChain: any;
        config: XDEFIConfig;
    }) => (chains: (typeof XDEFI_SUPPORTED_CHAINS)[number][]) => Promise<boolean>;
};

export { }
