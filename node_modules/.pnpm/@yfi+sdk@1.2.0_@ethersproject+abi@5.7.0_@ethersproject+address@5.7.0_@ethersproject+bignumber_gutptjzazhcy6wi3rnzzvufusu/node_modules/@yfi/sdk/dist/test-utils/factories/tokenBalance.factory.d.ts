import { TokenBalance } from "../..";
export declare const createMockTokenBalance: (overwrites?: Partial<TokenBalance>) => {
    address: string;
    priceUsdc: string;
    balance: string;
    balanceUsdc: string;
};
