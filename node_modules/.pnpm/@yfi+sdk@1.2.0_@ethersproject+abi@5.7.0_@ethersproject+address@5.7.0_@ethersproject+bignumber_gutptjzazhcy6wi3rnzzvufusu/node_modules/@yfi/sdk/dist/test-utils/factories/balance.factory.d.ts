import { Balance } from "../..";
export declare const createMockBalance: (overwrites?: Partial<Balance>) => {
    address: string;
    token: import("../..").ERC20;
    balance: string;
    balanceUsdc: string;
    priceUsdc: string;
};
