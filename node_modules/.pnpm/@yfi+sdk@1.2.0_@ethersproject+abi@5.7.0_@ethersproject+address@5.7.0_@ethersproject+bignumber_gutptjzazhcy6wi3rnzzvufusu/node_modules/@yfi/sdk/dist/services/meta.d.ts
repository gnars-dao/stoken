import { Service } from "../common";
import { StrategiesMetadata, TokenMetadata, VaultMetadataOverrides } from "../types";
/**
 * [[MetaService]] fetches meta data about things such as vaults and tokens
 * from yearn-meta
 */
export declare class MetaService extends Service {
    tokens(): Promise<TokenMetadata[]>;
    strategies(): Promise<StrategiesMetadata[]>;
    vaults(): Promise<VaultMetadataOverrides[]>;
    private buildUrl;
}
