import { RippleAPI } from 'ripple-lib';
import { BaseWorker } from '../baseWorker';
import type * as MessageTypes from '@trezor/blockchain-link-types/lib/messages';
declare class RippleWorker extends BaseWorker<RippleAPI> {
    pingTimeout?: ReturnType<typeof setTimeout>;
    cleanup(): void;
    protected isConnected(api: RippleAPI | undefined): api is RippleAPI;
    tryConnect(url: string): Promise<RippleAPI>;
    disconnect(): void;
    messageHandler(event: {
        data: MessageTypes.Message;
    }): Promise<true | undefined>;
    setPingTimeout(): void;
    onPing(): Promise<void>;
}
export default function Ripple(): RippleWorker;
export {};
//# sourceMappingURL=index.d.ts.map