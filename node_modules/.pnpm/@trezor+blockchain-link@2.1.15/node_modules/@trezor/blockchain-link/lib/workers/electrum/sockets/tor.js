"use strict";
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.TorSocket = void 0;
const base_1 = require("./base");
class TorSocket extends base_1.SocketBase {
    constructor(_a) {
        var { proxyAgent } = _a, rest = __rest(_a, ["proxyAgent"]);
        super(rest);
        this.proxyAgent = proxyAgent;
    }
    async openSocket(listener) {
        const { host, port } = this;
        const socket = await this.proxyAgent
            .callback(null, { host, port, timeout: this.timeout, secureEndpoint: false })
            .catch(e => {
            listener.onError(e);
            throw e;
        });
        listener.onConnect();
        this.configureSocket(socket);
        this.bindSocket(socket, listener);
        return socket;
    }
}
exports.TorSocket = TorSocket;
//# sourceMappingURL=tor.js.map