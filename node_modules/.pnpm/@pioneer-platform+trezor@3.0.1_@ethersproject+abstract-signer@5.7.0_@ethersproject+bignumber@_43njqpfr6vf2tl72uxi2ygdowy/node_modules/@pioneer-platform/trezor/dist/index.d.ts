import { Chain } from '@pioneer-platform/types';
import type { ConnectWalletParams } from '@pioneer-platform/types';
import type { DerivationPathArray } from '@pioneer-platform/types';

export declare const TREZOR_SUPPORTED_CHAINS: readonly [Chain.Avalanche, Chain.Bitcoin, Chain.BitcoinCash, Chain.Dogecoin, Chain.Ethereum, Chain.BinanceSmartChain, Chain.Litecoin];

export declare const trezorWallet: {
    connectMethodName: "connectTrezor";
    connect: ({ apis, rpcUrls, addChain, config: { covalentApiKey, ethplorerApiKey, utxoApiKey, trezorManifest, }, }: ConnectWalletParams) => (chain: (typeof TREZOR_SUPPORTED_CHAINS)[number], derivationPath: DerivationPathArray) => Promise<boolean>;
};

export { }
