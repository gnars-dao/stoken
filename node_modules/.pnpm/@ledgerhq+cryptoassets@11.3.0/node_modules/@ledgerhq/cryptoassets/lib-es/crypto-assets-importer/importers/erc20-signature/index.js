var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
import fs from "fs";
import path from "path";
import { fetchTokens } from "../../fetch";
export const importERC20Signatures = (outputDir) => __awaiter(void 0, void 0, void 0, function* () {
    console.log("importing erc20 signatures...");
    try {
        const erc20Signatures = yield fetchTokens("erc20-signatures.json");
        const filePath = path.join(outputDir, "erc20-signatures");
        if (erc20Signatures) {
            fs.writeFileSync(`${filePath}.json`, JSON.stringify(erc20Signatures));
            fs.writeFileSync(`${filePath}.ts`, `export default ${JSON.stringify(erc20Signatures, null, 2)};`);
            console.log("importing erc20 signatures: success");
        }
    }
    catch (err) {
        console.error(err);
    }
});
//# sourceMappingURL=index.js.map