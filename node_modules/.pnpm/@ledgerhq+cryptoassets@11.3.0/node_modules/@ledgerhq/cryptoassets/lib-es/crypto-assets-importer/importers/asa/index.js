var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
import fs from "fs";
import path from "path";
import { fetchTokens } from "../../fetch";
export const importAsaTokens = (outputDir) => __awaiter(void 0, void 0, void 0, function* () {
    try {
        console.log("importing asa tokens...");
        const asaTokens = yield fetchTokens("asa.json");
        const filePath = path.join(outputDir, "asa");
        fs.writeFileSync(`${filePath}.json`, JSON.stringify(asaTokens));
        fs.writeFileSync(`${filePath}.ts`, `export type AlgorandASAToken = [
  string, // id
  string, // abbr
  string, // name
  string, // contractAddress
  number, // precision
  boolean?, // enableCountervalues
];

import tokens from "./asa.json";

export default tokens as AlgorandASAToken[];
`);
        console.log("importing asa tokens sucess");
    }
    catch (err) {
        console.error(err);
    }
});
//# sourceMappingURL=index.js.map