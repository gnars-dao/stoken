"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.importSPLTokens = void 0;
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
const fetch_1 = require("../../fetch");
const importSPLTokens = (outputDir) => __awaiter(void 0, void 0, void 0, function* () {
    try {
        console.log("importing spl tokens...");
        const splTokens = yield (0, fetch_1.fetchTokens)("spl.json");
        const filePath = path_1.default.join(outputDir, "spl");
        const splTypeStringified = `export type SPLToken = [
  number, // chainId
  string, // name
  string, // symbol
  string, // address
  number, // decimals
  boolean?, // enableCountervalues
];`;
        fs_1.default.writeFileSync(`${filePath}.json`, JSON.stringify(splTokens));
        fs_1.default.writeFileSync(`${filePath}.ts`, `${splTypeStringified}

import tokens from "./spl.json";

export default tokens as SPLToken[];
`);
        console.log("importing spl tokens sucess");
    }
    catch (err) {
        console.error(err);
    }
});
exports.importSPLTokens = importSPLTokens;
//# sourceMappingURL=index.js.map