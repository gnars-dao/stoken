"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.importERC20 = void 0;
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
const fetch_1 = require("../../fetch");
const importERC20 = (outputDir) => __awaiter(void 0, void 0, void 0, function* () {
    try {
        console.log("importing ERC20 tokens....");
        const erc20 = yield (0, fetch_1.fetchTokens)("erc20.json");
        if (erc20) {
            const filePath = path_1.default.join(outputDir, "erc20");
            fs_1.default.writeFileSync(`${filePath}.json`, JSON.stringify(erc20));
            const erc20TokenTypeStringified = `export type ERC20Token = [
  string, // parent currency id
  string, // token
  string, // ticker
  number, // precision
  string, // name
  string, // ledgerSignature
  string, // contract address
  boolean, // disabled counter values
  boolean, // delisted
  (string | null)?, // countervalue_ticker (legacy)
  (string | null)?, // coumpound_for (legacy)
];`;
            const erc20TsFile = `${erc20TokenTypeStringified}

import tokens from "./erc20.json";

export default tokens as ERC20Token[];
`;
            fs_1.default.writeFileSync(`${filePath}.ts`, erc20TsFile);
            console.log("importing ERC20 tokens: success");
        }
    }
    catch (err) {
        console.error(err);
    }
});
exports.importERC20 = importERC20;
//# sourceMappingURL=index.js.map