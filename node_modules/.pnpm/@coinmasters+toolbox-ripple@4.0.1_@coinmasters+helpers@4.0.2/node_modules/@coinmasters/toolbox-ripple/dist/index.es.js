import { RequestClient as s, AssetValue as l } from "@coinmasters/helpers";
const n = "https://pioneers.dev", c = (t) => s.get(`${n}/api/v1/getAccountInfo/ripple/${t}`), r = async (t) => {
  console.log(t);
  let e = await s.get(
    `${n}/api/v1/getPubkeyBalance/ripple/${t[0].address}`
  );
  console.log("balance: ", e), console.log("balance: ", typeof e), e && e.error && (e = "0"), await l.loadStaticAssets();
  const o = l.fromStringSync("XRP.XRP", e);
  o.address = t[0].address, console.log("assetValueNative: ", o);
  let a = [o];
  return console.log("balances: ", a), a;
}, p = () => ({
  // transfer: (params: TransferParams) => transfer(params),
  getAccount: c,
  getBalance: r
  // // getFees,
  // sendRawTransaction,
});
export {
  p as RippleToolbox
};
